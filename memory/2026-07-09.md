# 2026-07-09 工作日志

## gold-stone-care-employee 员工端 uat 分支更新

分支 `20260709-my` 合并到 `uat`，共 5 个提交，48 个文件变更（+1625 / -407）。

### 提交记录
```
4cf6ebb Merge branch '20260709-my' into uat
96d520a fix: 登录页禁止返回首页
f45e084 fix: 订单状态修正 + 统计卡片背景 + 回顶功能 + 图标优化
303dbc5 feat: 我的页面开发 + 移除直播模块
dfbc789 feat: 订单页筛选功能 + 登录流程优化 + 图标资源替换
2210d6c feat: 订单页筛选重构 - 类型/状态胶囊按钮、多选接单、筛选联动
```

### 1. 订单页（orders）重构
- **数据扩展**：订单数据从 2 条扩展到 12 条，覆盖已下单/已完成/已取消三种状态
- **机构logo**：改用 SVG（organization.svg），去掉 `hasOrgLogo` 动态检测逻辑
- **统计卡片**：背景从 CSS 渐变改为 background.png 图片，圆角 32rpx
- **图标替换**：Tab 类型从 emoji（🛵🍽️）换成 SVG 图标（icon_send/dine 系列）
- **勾选框**：从 CSS 圆圈改为 SVG 图标（icon_checkbox_active/default）
- **订单状态**：修正 status-ordered 替代 status-pending，去掉 status-delivering
- **回顶按钮**：改用 `wx.pageScrollTo` API，阈值 600→300px，图标换 SVG（icon_top.svg）
- **布局调整**：订单列表从 scroll-view 改为普通 view，背景色白色→灰色 #F5F5F5
- **字号调大**：28rpx→32rpx 等全面调大，圆角统一 32rpx
- **下拉菜单**：宽度固定 252rpx，选项改为胶囊样式 188rpx×70rpx

### 2. 我的页面（profile）开发
- 从空占位页面开发为完整功能页
- 头部：机构 logo + 机构名称 + 用户信息（与订单页一致）
- 功能卡片：切换机构、退出登录（带图标和描述）
- 切换机构：底部弹窗，scroll-view 展示 11 个模拟机构，选中高亮
- 退出登录：确认弹窗 → 清除 token/refreshToken/userInfo → 跳转登录页
- 样式：橙色主题 #F89F3D，32rpx 圆角，safe-area 适配

### 3. 登录页（login）优化
- 新增自定义导航栏「快捷登录」（无返回按钮）
- Logo 从动态检测改为直接使用 login-logo.svg（374×200rpx）
- 登录失败弹窗：新增 search.png 插图（220×220rpx）
- 文案变更：「网络异常」→「没有查询到绑定信息，请联系工作人员」
- 按钮：「我知道了」→「确定」，尺寸 480×98rpx

### 4. 手机号登录页（login-phone）
- 图形验证码增强：canvas 绘制，rpx 自适应
- 输入过滤：手机号只允许数字（11位）、验证码只允许字母数字（4位）、短信验证码只允许数字（6位）

### 5. 直播模块移除
- 删除 `pages/live/` 页面（js/json/wxml/wxss）
- app.json 移除 live 页面路由
- TabBar 从 3 项改为 2 项（去掉直播 tab）
- 删除旧 tab 图标（tab-home.png / tab-home-active.png）

### 6. 图标资源更新
- **新增 20+ 资源**：alert.png、background.png、search.png、transfer.png + 15个 SVG 图标
- **删除**：tab-home.png、tab-home-active.png
- SVG 图标涵盖：日历、复选框、餐饮（active/default/tag）、外送（active/default/tag）、定位、logo、搜索、顶部、展开、登录logo、机构

### 7. 配置精简
- `project.config.json` 大幅精简（53行变更）
