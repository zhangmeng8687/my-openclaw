# 2026-07-27 工作记录

## 分支操作
- 早上 9:19 推送所有未同步的本地分支到远端
  - 家属端：20260716-UI、20260721-API
  - 员工端：20260709-order、20260717-request、20260720-API、20260721-fix、20260723-BUG
- 家属端从 uat 拉取新分支 `20260727-DATA`
- 员工端从 uat 拉取新分支 `20260727-BUG`（uat 有 6 文件更新 fast-forward）

## 家属端今日工作总结（20260727-DATA 分支）

### 一、问题修复
1. **缓存键双重前缀** — storage.js 自动添加 gs_family_ 前缀，但 config.js/constants.js 中的 key 已包含该前缀，导致实际存储为 gs_family_gs_family_token。已移除冗余前缀。
2. **导航栏显示异常** — visit-edit.json 设置了 navigationStyle: "custom" 但未实现自定义导航栏，已改为使用系统默认导航栏。
3. **用户名默认值** — 移除首页"新用户"硬编码默认值，改为使用接口返回数据。

### 二、接口对接
1. **就诊记录/病史/过敏** — visit-edit 页面对接 health-manage 服务的 CRUD 接口，替换原有 mock 数据。
2. **健康报告** — health-report-edit 页面对接 saveCheckup/deleteCheckup 接口（文档描述有误，体检接口实为报告接口）。
3. **餐饮订单** — restaurant 和 order-detail 页面正确解析 dishList 数组结构，修复字段映射。

### 三、功能优化
1. **病史/过敏按钮** — 根据是否有记录动态显示"添加"或"编辑"，编辑时通过 globalData 传递已有内容。
2. **订单详情重构** — 整合为单卡片布局，参考员工端样式：
   - 机构名称 + 状态标签（胶囊状）
   - 餐品信息区域
   - 订单信息区域
3. **状态标签样式** — 统一为 green/orange/grey 三色，与员工端一致。

## 员工端今日工作总结（20260727-BUG 分支）

### 1. 优化退出登录缓存逻辑
**问题：** 退出登录时清除所有缓存，导致再次登录无法定位到用户最后选择的机构
**解决方案：**
- 修改 `services/auth.js`、`app.js`、`utils/request.js` 三个文件
- 退出登录时只清除 token、refresh_token、user_info
- 保留 `current_org` 和 `org_list` 缓存

### 2. 订单列表空状态图片显示
**需求：**
- 无数据时显示 `images/01.png`
- 网络异常时显示 `images/02.png`
**修改页面：**
- `pages/orders/orders` — 订单管理页
- `pages/all-orders/all-orders` — 所有订单页
- `pages/order-search/order-search` — 订单搜索页
**技术实现：**
- 添加 `loadError` 状态区分无数据和网络异常
- 空状态图片移到 scroll-view 外部解决显示问题
- 使用 `wx:elif` 确保只显示一个图片
- 图片宽度 690rpx，高度自适应

### 3. 修复切换 Tab 空状态不显示
**问题：** 切换日期/类型/状态筛选时，旧数据未清空导致空状态判断失败
**解决：** 刷新请求前先清空列表数据

---
_最后更新：2026-07-27 18:12_
