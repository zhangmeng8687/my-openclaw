# 2026-07-22 工作记录

## MEMORY.md 重建
- 发现 MEMORY.md 从第一次提交（2026-06-23）开始就是乱码，所有中文内容损坏
- 原因：文件创建时经历了编码转换错误（系统默认 GB2312，文件以 UTF-8 存储）
- 从 memory/*.md 日志文件完整重建，内容涵盖 2026-06-17 ~ 2026-07-21 所有工作记录

## 代码注释乱码检查
- 员工端（gold-stone-care-employee）：65 个文件全部正常，无乱码
- 家属端（gold-stone-care-family）：发现并修复 1 个文件
  - `pages/health-manage/health-manage.wxss` — 2 处注释乱码（"导航�?"、"横屏下 rpx 放大�?"）

## 家属端购物车图标更新
- 文件：`pages/food-detail/food-detail`
- 图标从 `icon_cart.svg` 改为 `order.png`
- 图标尺寸：120rpx × 160rpx，允许超出底部购物车栏上方
- CSS 调整：`align-items: flex-end`、`overflow: visible`、`margin-top: -60rpx`

## 家属端调试日志完善
- elderly-manage.js 扫码流程：打印扫码结果、JSON 解析、bindToken、老人信息
- bind-confirm.js：打印页面参数、bindToken、提交参数、relation 映射
- request.js 已有完整日志（API 请求/响应/错误/401）

## 家属端首页逻辑优化
- 未绑定老人时隐藏功能入口（信息录入/健康管理/餐饮服务）
- 只显示"关爱老人"卡片和"绑定老人"按钮

## 家属端扫码绑定重构
- 二维码格式：JSON `{ bindToken, expireIn, realname, avatar, communityName }`
- elderly-manage.js 解析 JSON，提取老人信息传入 bind-confirm
- bind-confirm 页面展示老人头像、姓名、社群名称
- 页面标题改为"二维码Code"

## Git 分支
- 从 uat 新建 `20260722-API` 分支
- 所有变更在分支上，未提交

## 家属端绑定确认页重构
- 白色卡片：宽646rpx，居中，圆角32rpx
- 头像200rpx，距顶部160rpx，距名字64rpx
- 姓名48rpx，#333，字重500
- 社群36rpx，#666，距按钮240rpx
- 按钮480×98rpx，圆角86rpx，#FFD5AB背景，#632B13文字
- 两侧半圆缺口42rpx，橙色背景色
- 多层叠加：470rpx(0.2→0渐变) + 565rpx(0.2) + 646rpx白色卡片
- 距导航栏161rpx开始
- 页面标题改为"二维码Code"

## 扫码绑定流程重构
- 二维码格式：JSON { bindToken, expireIn, realname, avatar, communityName }
- elderly-manage.js 解析JSON，提取老人信息传入bind-confirm
- bind-confirm 页面展示老人头像、姓名、社群名称
- 全流程控制台打印调试信息

## 首页逻辑优化
- 未绑定老人时隐藏功能入口（信息录入/健康管理/餐饮服务）

## 错误处理统一（两端）
- 所有 console.error / console.warn → console.log
- 员工端8个文件 + 家属端10个文件

## 频繁重新登录修复（两端）
- 根因：forceLogout() 调 storage.clearAll() 清空所有缓存
- token 刷新机制在家属端不可用（缺 phoneCode）
- 修复：forceLogout() 和 clearLoginState() 只清 TOKEN_KEY 和 USER_INFO_KEY
- 保留老人列表/机构列表等业务缓存

## PowerShell 编码警告
- PowerShell 替换文件内容会破坏中文编码（GBK/UTF-8混用）
- 必须用 Node.js 或 write/edit 工具处理含中文的文件

## 登录问题修复
- 根因：app.json 第一页是 pages/login/login，app 启动总是先进登录页
- checkLogin() 有 token 时只恢复状态，没有跳首页
- 修复：checkLogin() 有 token 时加 wx.reLaunch 跳首页
- 缓存 key 问题：config.js 的 key 已有 gs_family_ 前缀，storage.js 再加一次导致双重前缀
- 已恢复原始 key 配置，保持 gs_family_gs_family_token
- 员工端首页是 pages/orders/orders，无此问题

## 老人管理页显示 userId
- 用户信息区名称下方显示 `ID: {{userInfo.userId}}`
- 样式保持胶囊状（橙色背景、圆角）

## 健康管理页修复
- 顶部区域固定：header-bg 加 position: fixed，页面加 padding-top: 380rpx
- 异常事件时间格式：slice(-5) → slice(11, 16)，正确显示"时:分"
- 异常事件名称：event.text → event.eventName，显示"设备检测出呼吸暂停"等

## 信息录入页导航栏对齐
- padding-top 从 statusBarHeight 改为 statusBarHeight + 44
- 与健康管理页保持一致，对齐微信胶囊

## 餐饮服务页字段映射修正
- /ma/dish/menu 接口：dishId、dishName、content、supplierName、supplierId、imageUrl
- /ma/dish/{dishId} 接口：同上 + comboContent、mealModeName、calorie、avgStar
- 餐品详情页：comboContent 按 + 拆分显示套餐明细
- 新增热量显示（calorie）
- 配餐机构改为 supplierName

## 购物车角标颜色
- 背景色从 #F89F3D（橙色）改为 #FF4D4F（红色）

## 餐饮服务页机构筛选修复
- 新增 allRestaurantList 存储全部餐厅列表
- 首次无筛选时更新 allRestaurantList
- 下拉菜单始终显示全部机构，选中后可取消回到全部

## 确认订单页顶部缝隙修复
- 原因：padding-top: env(safe-area-inset-top) 导致导航栏和内容之间有缝隙
- 修复：移除多余 padding，微信默认导航栏已处理安全区

## Git 提交
- 家属端 20260722-API 分支：扫码绑定重构 + 首页优化 + 字段映射 + 错误处理（25文件）
- 员工端 20260722-API 分支：错误处理统一 + 缓存key修复（9文件）
- 两端均已推送到 Gitee，未合并到 uat

---
_最后更新：2026-07-22 18:39_
