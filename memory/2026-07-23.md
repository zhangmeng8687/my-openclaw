# 2026-07-23 工作记录

## 金石云伴家属端（gold-stone-care-family）

### Git 分支管理
- 从 `20260722-API` 新建 `20260723-API` 分支
- stash → checkout → pop 流程，15 个文件变更迁移到新分支
- 变更文件：health-line-chart（3文件）、health-sleep-stage-chart（3文件）、elderly-manage（2文件）、health-manage（3文件）、health-record（1文件）、index（2文件）、profile（1文件）

### project.config.json appid 恢复
- 发现最新提交 `12bafba` 中 appid 被改为 `wx98b4a61d2e6fe94f`
- 原始远端 appid 为 `wx40cbd1fb4bb0441e`
- 用 `git checkout 8757276 -- project.config.json` 恢复
- 提交：`04e88f9 fix: 恢复 project.config.json appid 为远端原始值`

### 设计稿 #5.09 #5.10 #5.11 开发
- 设计稿 7/23 新增：5.09 录入信息-操作、5.10 上传报告、5.11 上传附件
- **5.09 - 文件操作弹窗**：
  - 原来用 `wx.showActionSheet`，只有预览/删除两项
  - 改为自定义底部弹窗：编辑报告/下载/预览（三按钮一行）+ 删除（红色）+ 取消
  - 新增 6 个 SVG 图标：icon_edit、icon_download、icon_preview、icon_delete_red、icon_upload_blue、icon_delete_gray
- **5.10/5.11 - 健康报告编辑页**：
  - 新建 `health-report-edit` 页面（4文件：wxml/wxss/js/json）
  - 表单字段：就诊医院、报告日期、备注
  - 附件区域：上传按钮 + 文件列表（PDF 红色图标 + 文件名 + 删除）
  - 底部操作栏：删除（灰色）+ 保存报告（橙色）
  - 注册到 app.json

### Canvas 层级问题修复
- 问题：`<canvas type="2d">` 是原生渲染层，CSS z-index 无效，滚动时遮挡导航栏
- 方案：Canvas 渲染完成后自动转为图片显示
  - `_canvasToImage()` 方法，延迟 300ms 调用 canvasToTempFilePath
  - `_converting` 标志防止多次同时转换
  - 图片加载完成后再隐藏 canvas，避免空白间隙
- 修改文件：health-line-chart（3文件）、health-sleep-stage-chart（3文件）

### 首页老人信息数据完善
- 补充 API 返回字段：社群、地址、性别等
- 字段名统一：community → communityName
- 性别字段：sex (1/2) → gender (男性/女性)
- 修改文件：index、elderly-manage、profile、health-record、health-manage

### 健康管理页面 UI 优化
- 异常事件卡片展开/收起动画：max-height + opacity 过渡
- 疾病/过敏标签拆分：按标点符号拆分成独立标签
- 就诊记录卡片样式：时间/医院名称字号 34、颜色 333、字重 500
- 时间戳转换：就诊记录时间转为 YYYY-MM-DD 格式

### 录入信息页面重构
- 顶部固定：导航 + 老人信息区域 position: fixed，Tab 栏不固定
- 卡片样式统一：宽度 678rpx，圆角 32rpx
- 健康报告卡片重设计：左侧图标 45rpx、报告名 34rpx、附件名 32rpx、时间 30rpx
- Tab 栏：去掉未选中白色背景

### 操作弹窗样式重做
- 灰色背景（#F5F5F5）弹出层
- 三个白色操作按钮（编辑报告、下载、预览）：210×179rpx，间距 24rpx
- 删除按钮：文本左、图标右
- 取消按钮：白色背景通屏，包含底部安全区

### 列表页分页加载
- all-orders、order-search、food-search 添加 onReachBottom 触底加载
- 每页 20 条，显示加载状态提示

### 错误处理优化
- 移除重复错误提示：request.js 已统一处理
- 修复 Babel 兼容问题：展开运算符改为 concat/Object.assign

### 其他修改
- visit-edit.json 添加 navigationStyle: "custom"
- health-report-edit.json 添加 navigationStyle: "custom"
- 创建 icon_calendar.svg 日历图标

## 金石云伴员工端（gold-stone-care-employee）

### Git 分支管理
- `20260722-API` 合并到 `uat`（fast-forward，9文件变更）
- 新建 `20260723-BUG` 分支，基于最新 uat

### Bug 修复
- order-search.js：`util` 未引入导致 `ReferenceError`，补上 `require('../../utils/util')`

### 今日工作总结（员工端）

#### 1. 错误处理统一化
- 统一网络异常错误提示，由 request.js 统一处理
- 删除各页面重复的"加载失败"提示
- 涉及页面：all-orders、orders、order-detail、profile、order-search

#### 2. 登录模块优化
- 添加已登录状态检查，已登录用户自动跳转首页
- 调整协议勾选区域位置到页面顶部
- 优化协议勾选区域样式对齐
- 修复登录页样式对齐设计稿

#### 3. 订单功能完善
- 订单多选状态校验、堂食/外送状态分离
- 批量接单/送达接口对接
- 订单详情经纬度字段对齐后端
- 修复PC端微信无法获取订单id的问题
- 堂食单仅展示，隐藏所有操作按钮

#### 4. 搜索页面优化
- 补充下拉刷新功能
- 修复搜索页面高度溢出问题
- 优化搜索结果列表高度计算
- 去除BOM头

#### 5. 首页优化
- 启动时优先恢复上次选中的机构
- 首页订单状态优化
- 验证码限制优化

#### 技术要点
- request.js 统一处理网络异常，业务错误保持页面自定义提示
- 登录态检查优化、机构选择状态持久化、订单筛选状态分离
- 协议勾选区域位置调整、搜索页面高度修复、样式对齐设计稿

#### 涉及页面
- 登录页（login、login-phone）
- 订单页（orders）、所有订单页（all-orders）
- 订单详情页（order-detail）、订单搜索页（order-search）
- 个人中心页（profile）

#### 今日提交统计
- 总提交数：80+ 次
- 主要分支：20260723-BUG、uat

---
_最后更新：2026-07-23 18:58_
