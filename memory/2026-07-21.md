# 2026-07-21 工作记录

## 金石云伴小程序

### 家属端 UI 优化
- 数量选择器：文字按钮替换为 SVG 图标（icon_reduce_default/icon_reduce_active/icon_add_default）
- 餐饮筛选：价格选中态背景 #FFEBD4 字体 #FDA159，机构选中箭头变白

### 员工端 Bug 修复
- **PC端订单详情拿不到 id**：`triggerEvent('tap')` 与原生 tap 事件冲突，改为 `triggerEvent('cardtap')`
  - 影响：orders/all-orders/order-search 三个页面 + order-card 组件
  - 根因：PC端微信原生 tap 事件干扰自定义事件传递

### 家属端登录对接真实 API
- 创建 `services/auth.js`（ma 模块）
- login.js 改为 `auth.wxLogin()` 真实接口
- login-phone.js 改为接口图片验证码 + `auth.loginByPhone()`
- app.js 新增 `checkLogin()` 登录态检查
- **关键发现**：家属端(ma)和员工端(ema)接口完全不同
  - 接口前缀：ma vs ema
  - 认证头：`X-MiniApp-Token` vs `X-Access-Token`
  - 响应字段：`result` vs `data`
  - phoneLogin 参数名：`phone` vs `mobile`

### 家属端全面对接 API（7个 service + 12个页面）
- **新增 service 文件**：
  - `bind.js`：家属绑定（申请/列表/选择老人/解绑）
  - `dish.js`：餐饮菜单（菜单查询/餐品详情/按餐段查询）
  - `order.js`：订单管理（下单/列表/详情/取消）
  - `review.js`：评价（提交/查看/删除/菜品评价列表）
  - `sleep.js`：睡眠数据（日报/最新报告/趋势）
  - `health.js`：健康档案（聚合查询/下载报告）
  - `health-manage.js`：健康信息 CRUD（就诊/病史/过敏/体检）
- **改造页面**：restaurant/food-detail/food-search/order-confirm/all-orders/order-detail/order-search/bind-confirm/elderly-manage/index/health-manage/health-record
- 删除旧 `services/elderly.js`（功能合并到 bind.js）

### 接口参数修正
- `/ma/phoneLogin` 参数名 `mobile` → `phone`（与员工端不同）
- `/ma/login` 的 `phoneCode` 从可选改为必填（文档要求）
- `/ma/dish-order/{id}/cancel` 的 `reason` 从 body 移到 query 参数
- `/ema/login` 的 `phoneCode` 同步修正为必填
- **静默登录限制**：/ma/login 要求 loginCode + phoneCode 均必填，token 过期无法静默刷新，会跳登录页

### 域名修正 + 文件清理
- 家属端 baseUrl：`js-test.zjmiit.com` → `js-test.zjmiit.com/wz`（与员工端一致）
- 删除 docs/API清单.md、测试 PDF
- 修复 JSON 文件 BOM 问题（PowerShell Set-Content -Encoding UTF8 自动加 BOM）

### 下拉刷新
- 家属端 8 个列表页添加整页下拉刷新（enablePullDownRefresh + onPullDownRefresh）
- 员工端 order-search 页补充下拉刷新
- 尝试改局部 scroll-view refresher，张总要求改回整页刷新

### 员工端首页订单筛选优化
- 外送单 tab：只显示待接单(status=0)订单，隐藏"待接单"状态标签
- 我的接单 tab：只显示我接取的外送单，传 `diningMethod=takeout` + `status=1` + `acceptorId=当前userId`

## Git 操作
- 员工端：直接在 uat 分支 commit + push（3次）
- 家属端：20260716-UI commit → 合并 uat → push → 新建 20260721-API 分支
- 家属端 20260721-API 分支：5次 commit

## 接口文档
- 文档位置：D:\projects\小程序接口文档v1.docx
- 用 PowerShell 解压 docx 读取 XML 提取文本
- 7 大模块：登录(ma)/家属绑定/餐饮菜单/订单管理/评价/睡眠健康/健康信息管理
