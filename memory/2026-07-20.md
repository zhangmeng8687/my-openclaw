# 2026-07-20 工作记录

## 金石云伴员工端（gold-stone-care-employee）

### 分支：20260720-API（从 20260717-request 合并后新建）

**1. 登录修复**
- `saveLoginData` 适配接口扁平响应结构：`{ token, name, avatar, userId }`
- 原逻辑只取 `result.userInfo`，导致 name 丢失
- 现在兼容扁平和嵌套两种格式，自动构建 userInfo
- 放开微信一键登录（移除 doWxLogin 中的调试 `return`）

**2. 订单状态判断优化**
- 送达按钮根据订单状态调用不同接口：
  - 待接单(0) → `batchAccept`
  - 已接单(1) → `batchDeliver`
  - 配送中(2) → `batchFinish`
- orders 页和 all-orders 页均已更新
- UI 不变（按钮始终显示「接单」/「送达」），只有底层接口调用变化

**3. 首页筛选传参**
- `loadOrders` 新增 `diningMethod` 参数，按外送/堂食筛选
- 「我的接单」Tab 不传 `diningMethod`，查全部类型订单

**4. 堂食单地址显示**
- 堂食单无送餐地址，改为显示 `supplierName`（机构名称）
- 四个页面同步更新：orders、all-orders、order-search、order-detail

**5. 订单详情页优化**
- 经纬度字段对齐后端：`latitude` → `deliveryLat`，`longitude` → `deliveryLng`
- 时间格式改为 `YYYY年MM月DD日 HH:mm:ss`（下单时间、送达时间、取消时间）
- 取消订单按钮：除已完成/已取消外均显示（原来只在待接单状态显示）
- 「订单已取消」标题移至卡片外部，卡片内只保留取消时间和取消原因

**6. 堂食单只展示不操作**
- order-card 组件：多选框仅外送单显示（已有 `orderType === 'takeout'` 判断）
- order-detail 页：操作栏加 `diningMethod === 'takeout'` 判断，堂食单隐藏所有按钮
- 堂食单不能多选、不能接单、不能取消

**7. 其他调整**
- 返回顶部按钮阈值：300 → 1000px
- 堂食单筛选文案：「已下单」→「待接单」（值不变）

**提交记录：**
- `fix: 订单多选状态校验、堂食/外送状态分离、批量接单/送达`（12文件，282行）
- `fix: 登录态保存适配扁平结构、订单状态判断优化、详情页时间格式与取消订单显示`（8文件，122行）
- `fix: 堂食单仅展示，隐藏所有操作按钮`（1文件）
- `fix: 订单详情经纬度字段对齐后端 deliveryLng/deliveryLat`（1文件）
- 全部合并到 uat 并 push

---

## 金石云伴家属端（gold-stone-care-family）

### 分支：20260716-UI

**1. 确认订单页（order-confirm）优化**
- 地址行添加 label：根据用餐形式显示「外送地址」/「用餐地址」
- 确认下单按钮尺寸改为 600×88rpx，居中显示
- 字号调整：表单标签/值 32rpx、套餐名称 34rpx、数量 36rpx、价格 40rpx
- 餐品卡片布局调整：
  - 图片 112rpx，圆角 32rpx
  - 描述不省略，最多 2 行
  - 名称/描述行间距 16rpx
  - 标签在描述和价格之间，胶囊状（border-radius: 999rpx），字号 30rpx
  - 标签到价格距离 36rpx

**2. 家属端 UI 对比整理**
- 完成全面对比，整理问题清单：
  - 严重：orders/health 页空占位、全页面无真实接口
  - 功能缺失：13 项（全 mock 数据，services/ 只有 elderly.js）
  - UI 层面：4 项需确认
- 家属端暂不对接接口，先完善 UI

---

## 待处理
- [ ] 员工端：堂食单经纬度问题（后端是否返回机构坐标）
- [ ] 员工端：login.js 的 return 已移除，但 doWxLogin 需要实际测试
- [ ] 家属端：所有页面接口对接（等张总安排）
- [ ] 家属端：确认订单页餐品标签等接口返回正确数据后补上
