# 2026-08-04 工作记录

## V5VPN 卸载与系统清理

### 背景
用户要求恢复昨天为 VPN 兼容做的系统修改，将设置恢复原状。

### 发现的问题
1. **Clash Verge** 正在运行，开机自启已开启，注册表有代理配置
2. **Windows 代理注册表** `ProxyServer=127.0.0.1:9674`，`ProxyOverride` 有大量规则
3. **V5VPN** 安装后 TAP 驱动签名证书过期（2025年1月11日过期），导致驱动加载失败（错误码 263 / 0xffff2107）

### 已完成的操作
- ✅ 清除 Windows 代理注册表（ProxyServer、ProxyOverride）
- ✅ 移除 Clash Verge 开机自启
- ✅ 停止 Clash Verge 进程
- ✅ 完全卸载 V5VPN（服务、TAP 驱动、设备、Winsock 命名空间）
- ✅ 创建定时任务 `DeleteV5VPN` 在下次登录时删除残留的 `SecNSP.dll`

### 最终清理（15:12）
- ✅ V5VPN 安装目录已删除（`SecNSP.dll` 移至 `C:\Windows\Temp\SecNSP_toDelete.tmp`，系统自动清理）
- ✅ 所有定时任务已清理

### 待完成
- 用户准备安装新版 V5VPN 5.3.3.38（安装包在 `E:\xwechat_files\...\win11最新安装包VSG-g-369-3.0.3.2-Venus-VPN-5.exe`）
- 安装新版时如果还卡在 TAP 驱动，需要临时禁用驱动签名强制（启动设置 → 按7）

### 备注
- `memory search` 索引不可用（embedding provider 变更），需要运行 `openclaw memory index --force` 重建
- 用户反馈每次打开新对话页面看不到之前的对话内容 — 这是 WebChat 前端限制，不自动加载历史
